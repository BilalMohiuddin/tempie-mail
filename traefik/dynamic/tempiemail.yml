http:
  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: https
        permanent: true
  routers:
    http-catchall:
      rule: HostRegexp(`{any:.+}`)
      entryPoints:
        - web
      middlewares:
        - redirect-to-https
